# Con el proxy configurado en vite.config.js, las requests van a /api (mismo origen)
# y Vite las reenvía a localhost:3000. No hace falta URL absoluta.
# Dejar vacío para que api.js use el fallback "/api".
VITE_API_URL=
